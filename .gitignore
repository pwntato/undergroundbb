# Go
/bootstrap
/lambda.zip
*.test
vectors.log
# Root-anchored: ignores a stray compiled binary from
# `go build ./internal/crypto/testdata/gen`, not the tracked generator
# source at internal/crypto/testdata/gen/. Do not remove the leading slash.
/gen

# Node
node_modules/
dist/
.vite/
web/vectors-web.json

# Terraform
.terraform/
*.tfstate
*.tfstate.*
terraform.tfvars
.terraform.lock.hcl

# Env / secrets
.env
.env.local

# OS / editor
.DS_Store
.idea/
*.swp
